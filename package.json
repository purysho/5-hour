{
  "name": "driftless",
  "version": "0.0.0",
  "private": true,
  "description": "Detects breaking changes in upstream APIs and opens the fixing PR in downstream codebases.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "db:migrate": "node --experimental-strip-types scripts/migrate.ts",
    "db:reset": "node --experimental-strip-types scripts/migrate.ts --reset",
    "db:provider": "node --experimental-strip-types scripts/create-provider.ts",
    "db:watch": "node --experimental-strip-types scripts/watch-package.ts",
    "start:worker": "node --experimental-strip-types src/main/run-worker.ts",
    "start:server": "node --experimental-strip-types src/main/run-server.ts",
    "start": "pnpm start:server",
    "preflight": "node --experimental-strip-types scripts/preflight.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "@aws-sdk/client-kms": "^3.1111.0",
    "pg": "^8.13.1",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  }
}
